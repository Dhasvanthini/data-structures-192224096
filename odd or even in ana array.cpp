#include <stdio.h>
int main()
{
	int n,i;
	printf("enter the number:");
	scanf("%d",&n);
	int arr[n];
 printf("enter the %d elements in an array");
	for( i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
}
	printf("the even numbers in an array are:");
    {
	
    if(arr[i]%2==0)
	for(int i=0;i<n;i++)
    printf("%d",arr[i]);
}
	    printf("the odd numbers in an array:");
	 {
	 
	 if(arr[i]%2==1)
		for(int i=0;i<n;i++)
	printf("%d",arr[i]);
}
    
}
